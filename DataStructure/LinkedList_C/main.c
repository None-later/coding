#include <stdlib.h>
#include <stdio.h>
//#include "list.h"
//#include<time.h>

//void PrintList(const List L)
//{
//	Position P = Header(L);
//	if (IsEmpty(L))
//		printf("Empty List\n");
//	else
//	{	
//		do 
//		{
//			P = Advance(P);
//			printf("%d ", Retrieve(P));	
//		}
//		while (!IsLast(P));
//		printf("\n");
//	}
//}
struct ListNode 
{
	int val
}



int main (void)
{
	//List L;
	//Position P;
	////int i;
	//L = MakeEmpty(NULL); 
	//P = Header(L);

	//Insert(5, L, P);
	//Insert(1, L, P);
	//Insert(7, L, P);
	//Insert(2, L, P);
	//Insert(10, L, P);
	//Insert(13, L, P);
	//PrintList(L);
	////ReversePrint(Advance(L));
	//
	////Sort(L);
	////InsertToSorted(7, L);
	//
	////ReverseList(L);
 //   //PrintList( L );

	//Position Copy;
	////List Copy; 
	////Copy =	CopyList((struct Node*)L); 
	////PrintList(Copy);
	////DeleteList(Copy);

	//DeleteList( L );
    return 0;
}